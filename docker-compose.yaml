version: '3.8'
services:
  spark-etl:
    build: .
    environment:
      - SPARK_MODE=local
    # Uncomment the following lines to mount a local output directory
    # volumes:
    #   - ./output:/app/output